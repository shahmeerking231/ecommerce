<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/daisyui@5"
      rel="stylesheet"
      type="text/css"
    />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Add Product</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      html,
      body {
        width: 100%;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: rgb(31, 31, 31);
        flex-direction: column;
      }
      form {
        padding-bottom: 3vh;
        padding: 4vh;
      }
      .heading {
        width: 100%;
        height: 10%;
        padding-top: 5vh;
        display: flex;
        justify-content: center;
        align-items: end;
      }
      .heading h2 {
        font-size: 3vh;
        font-weight: 600;
        color: black;
      }
      input {
        padding: 2vh;
      }
      button {
        margin-top: 4vh;
        width: 100%;
        padding: 2vh;
      }
    </style>
  </head>
  <body>
    <form
      class="fieldset bg-base-200 border-base-300 rounded-box w-xs border p-6"
      enctype="multipart/form-data"
      action="/products"
      method="post"
    >
      <div class="heading">
        <h2>Add Product</h2>
      </div>
      <label class="label" for="name">Name</label>
      <input class="input w-full" type="text" id="name" name="name" />

      <label class="label" for="description">Description</label>
      <input
        class="input w-full"
        type="text"
        id="description"
        name="description"
      />

      <label class="label" for="price">Price</label>
      <input class="input w-full" type="number" id="price" name="price" />

      <label class="label" for="imageURL">Image Url</label>
      <input class="input w-full" type="file" id="imageURL" name="image" />

      <label class="label" for="quantity">Quantity</label>
      <input class="input w-full" type="number" id="quantity" name="quantity" />

      <button class="btn btn-neutral" type="submit">Add Product</button>
    </form>
  </body>
</html>
